# رصام ۲

دستیار پرسش‌وپاسخ روی دیتابیس که برای هر مشتری سفارشی می‌شود و روی سرور ما اجرا می‌شود. API اول، رابط گرافیکی بعداً. دیتابیس هدف فعلی: **MongoDB**.

## قبل از هر کاری

`PLAN.md` را بخوان. حافظه‌ی پروژه است: چشم‌انداز محصول، معماری، قواعد غیرقابل مذاکره، و فهرست قدم‌ها با وضعیتشان. جدول «اسناد پروژه» در ابتدایش می‌گوید هر قدم به کدام بخش از `docs/v1-reference.md` نیاز دارد.

## پروتکل کار

۱. در هر نوبت فقط **یک قدم** پیاده می‌شود، نه بیشتر. قدم بعدی را از `PLAN.md` بردار.
۲. بعد از هر قدم **بایست**. کاربر کد را می‌خواند و تایید می‌کند. تا تایید نگرفته‌ای سراغ قدم بعدی نرو.
۳. بعد از تایید، `PLAN.md` را به‌روز کن: وضعیت قدم، تصمیم‌های تازه در «لاگ تصمیم‌ها»، و هر انحرافی از نقشه.
۴. اگر تصمیمی لازم شد که در `PLAN.md` نیست، اول بپرس — حدس نزن.
۵. اندازه‌ی هر قدم حدود ۱۰۰ تا ۲۵۰ خط کد است؛ اندازه‌ای که در یک نشست خوانده و فهمیده شود. اگر قدمی دارد بزرگ می‌شود، بایست و بگو.
۶. هرجا تغییرات به نظرت آماده‌ی کامیت‌شدن‌اند، صریحاً بگو که باید کامیت شود، و همراهش یک خلاصه‌ی کوتاه از پیام کامیت را **به انگلیسی** هم بده (برای استفاده‌ی مستقیم در `git commit -m`).

## سبک کد

- کامنت‌ها و docstringها **فارسی**، و توضیح‌دهنده‌ی **چرایی** تصمیم‌ها — نه شرح خط‌به‌خط کد.
- نام متغیرها و توابع انگلیسی. پیام‌های خطای رو به کاربر فارسی.
- متن پرامپت‌هایی که به مدل می‌روند انگلیسی، مگر جایی که صریحاً پاسخ فارسی خواسته می‌شود.
- کد باید **بهینه و ساده** باشد؛ از پیچیدگی غیرضروری (انتزاع زودهنگام، لایه‌های اضافه، راه‌حل‌های هوشمندانه‌ی غیرلازم) پرهیز شود تا خوانایی کد برای کاربر کم نشود. ساده‌ترین راه‌حلی که نیاز قدم را برآورده می‌کند، ترجیح دارد.

## قواعد غیرقابل مذاکره

شرح کامل در بخش ۵ `PLAN.md`. خلاصه:

- **هیچ خروجی مدل هرگز اجرا نمی‌شود** — نه `exec`، نه `eval`، نه `$where`، نه `$function`، نه `$accumulator`.
- اعتبارسنجی کوئری پیش از اجرا **اجباری** است و در گراف قابل دور زدن نیست.
- لایه‌ی نمایش (API) هیچ منطقی ندارد؛ فقط `Answer` را سریالایز می‌کند.
- هیچ مسیر، کلید یا نام مدلی hard-code نمی‌شود.
- هر متن غیرقابل‌اعتماد پیش از ورود به پرامپت محصور، پاک‌سازی و کوتاه می‌شود.
- هیچ تابع سطح بالایی exception پرتاب نمی‌کند؛ خطا داخل `Answer` برمی‌گردد.
- هر قدم باید مستقل قابل تست باشد.

## دستورهای پروژه

```powershell
.\.venv\Scripts\Activate.ps1   # فعال‌کردن محیط مجازی
pytest                          # تست‌ها
docker compose up -d            # مونگوی محلی توسعه
```
