# وابستگی‌های رصام ۲ — گروه‌بندی بر اساس قدمی که واقعاً لازمشان می‌کند.
# دلیل گروه‌بندی: اگر نصب یکی از بسته‌های سنگین روی پایتون ۳٫۱۳ مشکل داشت،
# بدانیم کدام قدم را بلاک می‌کند و کدام قدم‌ها بی‌خیال ادامه می‌دهند.

# --- قدم ۱: تنظیمات و پروفایل ---
pydantic>=2.7
pydantic-settings>=2.2
PyYAML>=6.0
python-dotenv>=1.0

# --- قدم ۲ و ۳: مونگو و داده ---
pymongo>=4.7
pandas>=2.2

# --- قدم ۵ تا ۸: مدل زبانی و گراف ---
langchain-core>=0.3
langchain-openai>=0.2
langgraph>=0.2

# --- قدم ۶: حافظه‌ی دانش ---
chromadb>=0.5
langchain-chroma>=0.1

# --- قدم ۱۰: نمودار ---
plotly>=5.22

# --- قدم ۱۳: API ---
fastapi>=0.111
uvicorn[standard]>=0.30

# --- تست ---
pytest>=8.2
