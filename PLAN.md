# رصام نسخه‌ی ۲ — سند معماری و نقشه‌ی قدم‌ها

> **این فایل حافظه‌ی پروژه است.**
> پیش از شروع هر قدم، این فایل خوانده می‌شود. پس از اتمام هر قدم، بخش «وضعیت فعلی» و «لاگ تصمیم‌ها» به‌روزرسانی می‌شود.
> اگر جلسه‌ای قطع شد یا مدل عوض شد، خواندن همین فایل باید برای ادامه‌ی کار کافی باشد.

### اسناد پروژه

| فایل | چیست | کِی خوانده شود |
|---|---|---|
| `PLAN.md` | همین سند: چشم‌انداز، معماری، قواعد، قدم‌ها | **همیشه، اول از همه** |
| `docs/v1-reference.md` | دانش منتقل‌شده از نسخه‌ی ۱: دانش دامنه، متن پرامپت‌های آزموده‌شده، کشف‌های عملیاتی، و بدهی‌هایی که نباید تکرار شوند | در قدم‌های ۳، ۵، ۶، ۷، ۸، ۹، ۱۰ — هر قدم می‌گوید کدام بخش |

**کد نسخه‌ی ۱ در دسترس نیست و لازم هم نیست.** هرچه از آن ارزش داشت در `docs/v1-reference.md` استخراج شده. اگر چیزی لازم شد که آنجا نیست، به‌جای حدس زدن بپرسید.

---

## وضعیت فعلی

| مورد | مقدار |
|---|---|
| آخرین قدم تمام‌شده | **قدم ۲ — محیط MongoDB و داده‌ی نمونه** (تایید شد) |
| قدم بعدی | قدم ۳ — استنتاج schema از MongoDB |
| دیتابیس هدف فعلی | MongoDB |
| ایجنت‌های پیاده‌شده | — |
| تاریخ آخرین به‌روزرسانی | ۱۴۰۵/۰۶/۱۱ |

---

## ۱. چشم‌انداز محصول (این را هرگز فراموش نکن)

رصام یک **محصول** است، نه یک اسکریپت. مشخصاتش:

- **مشتری‌محور:** برای هر مشتری جداگانه سفارشی می‌شود — بسته به نوع دیتابیسش، محتوای دیتابیسش (schema و واژگان کسب‌وکارش) و قابلیت‌هایی که خریده است.
- **قابلیت‌ها انتخابی‌اند:** یک مشتری فقط جدول نتیجه می‌خواهد، دیگری نمودار هم می‌خواهد، سومی خروجی Excel. اینها باید با **کانفیگ** روشن و خاموش شوند، نه با تغییر کد.
- **روی سرور ما اجرا می‌شود.** یعنی داده‌ی مشتری از سرور ما عبور می‌کند و امنیت یک نیازمندی درجه‌یک است، نه یک ویژگی خوب.
- **API اول، رابط گرافیکی بعداً.** پس هیچ منطقی نباید در لایه‌ی نمایش باشد. لایه‌ی نمایش باید بتواند بدون تغییر منطق عوض شود.
- **چند دیتابیس:** MongoDB اولی است، اما معماری از روز اول باید طوری باشد که افزودن PostgreSQL یا هر دیتابیس دیگر، **یک فایل جدید** باشد نه یک بازنویسی.

### چرا وانّا حذف می‌شود

وانّا کتابخانه‌ی خوبی است اما یک فرض بنیادی دارد: **خروجی، SQL است.** این فرض در `VannaBase` سیم‌کشی شده — در نام متدها (`generate_sql`, `is_sql_valid`, `run_sql`)، در ساختار vector store (کالکشنی به نام `sql`)، و در پرامپت‌ها. برای محصولی که باید MongoDB و بعداً هر چیز دیگری را ساپورت کند، این فرض یک سقف است نه یک پایه.

ضمناً در نسخه‌ی ۱ مجبور شدیم چهار متد وانّا را override کنیم که سه‌تایشان دلیل امنیتی داشت (`is_sql_valid` حفره داشت، `generate_summary` کل جدول را می‌فرستاد، مسیر نمودار `exec` می‌کرد). وقتی بخش زیادی از یک کتابخانه را باید کنار بزنید، دیگر کتابخانه به شما کمک نمی‌کند.

**اما آنچه وانّا درست انجام می‌داد را نگه می‌داریم:** بازیابی معنایی دانش (فقط مرتبط‌ترین schema و مستندات و مثال‌ها وارد پرامپت شوند، نه همه‌چیز). این تفاوت بین کارکردن و کارنکردن است وقتی مشتری ۵۰ جدول/کالکشن دارد.

---

## ۲. ایده‌ی مرکزی معماری

> **هرچه مخصوص یک دیتابیس است داخل «ایجنت» آن دیتابیس می‌رود. هرچه مشترک است بیرون می‌ماند و هرگز تکرار نمی‌شود.**

پشتیبانی از یک دیتابیس جدید = نوشتن **یک فایل ایجنت جدید** + ثبت آن در رجیستری. هیچ فایل دیگری از پروژه دست نمی‌خورد.

```
┌─────────────────────────────────────────────────────────────┐
│  مشترک بین همه‌ی دیتابیس‌ها (یک بار نوشته می‌شود)              │
│                                                             │
│   گراف LangGraph  ·  بازیابی دانش  ·  خلاصه‌سازی             │
│   نمودار  ·  پروفایل مشتری  ·  قابلیت‌ها  ·  API  ·  Answer    │
└───────────────────────────┬─────────────────────────────────┘
                            │  قرارداد DatabaseAgent
            ┌───────────────┼───────────────┬─────────────┐
            ▼               ▼               ▼             ▼
     ┌─────────────┐ ┌─────────────┐ ┌───────────┐  ┌─────────┐
     │ MongoAgent  │ │PostgresAgent│ │MySQLAgent │  │ApiAgent │
     │  (قدم ۷)    │ │  (قدم ۱۴)   │ │  (بعداً)  │  │ (بعداً) │
     └─────────────┘ └─────────────┘ └───────────┘  └─────────┘
```

### قرارداد `DatabaseAgent`

هر ایجنت یک کلاس است که این اعضا را دارد — نه بیشتر، نه کمتر:

| عضو | کار | مثال در MongoAgent |
|---|---|---|
| `db_type` | شناسه‌ی متنی برای رجیستری | `"mongodb"` |
| `introspect()` | ساختار داده را از خود دیتابیس بخواند | نمونه‌گیری از داکیومنت‌ها و استنتاج فیلدها و نوعشان |
| `describe_schema()` | ساختار را به متنی که به مدل داده می‌شود تبدیل کند | فهرست کالکشن‌ها، فیلدها، نوع‌ها، مقادیر نمونه |
| `query_language` | نام و توضیح زبان کوئری برای پرامپت | `"MongoDB aggregation pipeline (JSON)"` |
| `build_query(question, context)` | از مدل کوئری بگیرد و parse کند | JSON pipeline |
| `validate_query(query)` | **دروازه‌ی امنیتی** — فهرست سفید | بررسی هر stage و operator |
| `execute(query)` | اجرای فقط-خواندنی و برگرداندن DataFrame | `collection.aggregate(...)` |

نکته‌ی مهم: `validate_query` **داخل** ایجنت است چون قواعد امنیتی هر دیتابیس فرق دارد (در SQL نگران `DROP` هستیم، در Mongo نگران `$where` و `$function` و `$out`). اما **اینکه اعتبارسنجی الزامی است** یک قاعده‌ی مشترک و غیرقابل دور زدن در گراف است.

---

## ۳. گراف LangGraph

یک گراف مشترک، با یک `State` صریح. ایجنت مثل یک قطعه‌ی قابل تعویض داخل گره‌ها استفاده می‌شود.

```
        ورودی: سوال کاربر + شناسه‌ی مشتری
                        │
                        ▼
        ┌───────────────────────────────┐
        │ load_profile                  │  پروفایل مشتری → کدام ایجنت، کدام قابلیت‌ها
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ retrieve_knowledge            │  مشترک: schema + مستندات + مثال‌های مرتبط
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ generate_query                │  ← agent.build_query()
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ validate_query                │  ← agent.validate_query()   ⛔ دروازه‌ی اجباری
        └───────┬───────────────┬───────┘
            رد شد           قبول شد
                │               ▼
                │   ┌───────────────────────────┐
                │   │ execute                   │  ← agent.execute()
                │   └───────┬───────────┬───────┘
                │        خطا داد      موفق
                │           │            │
                │           ▼            │
                │   ┌──────────────┐     │
                │   │ repair       │─────┘  خودترمیمی، حداکثر ۲ تلاش
                │   │ (اختیاری)    │        (قدم ۱۲)
                │   └──────────────┘
                │                        ▼
                │            ┌───────────────────────┐
                │            │ summarize  (اگر مجاز) │
                │            └───────────┬───────────┘
                │                        ▼
                │            ┌───────────────────────┐
                │            │ chart      (اگر مجاز) │
                │            └───────────┬───────────┘
                ▼                        ▼
        ┌───────────────────────────────────────┐
        │            Answer                     │
        └───────────────────────────────────────┘
```

گره‌های `summarize` و `chart` بر اساس **قابلیت‌های مشتری** روشن یا خاموش می‌شوند — نه با `if` پراکنده در کد، بلکه در ساخت گراف.

---

## ۴. پروفایل مشتری

هر مشتری یک پوشه دارد. کد مشترک است؛ فقط این پوشه عوض می‌شود.

```
customers/
└── acme/
    ├── profile.yaml          ← نوع دیتابیس، اتصال، قابلیت‌ها، سقف‌ها
    ├── knowledge/
    │   ├── documentation.md  ← واژگان کسب‌وکار و قواعد دامنه
    │   └── examples.yaml     ← جفت‌های «سوال ← کوئری درست»
    └── vector_store/         ← دانش آموزش‌دیده (خودکار)
```

نمونه‌ی `profile.yaml`:

```yaml
name: acme
database:
  type: mongodb              # ← کدام ایجنت بارگذاری شود
  uri_env: ACME_MONGO_URI    # نام متغیر محیطی، نه خود رمز
  database: solar_telemetry
capabilities:
  table: true
  summary: true
  chart: true
  export_csv: false
limits:
  max_rows: 1000
  query_timeout_seconds: 15
language: fa
```

> **قاعده:** رمز و connection string هرگز داخل `profile.yaml` نوشته نمی‌شود — فقط **نام** متغیر محیطی. پروفایل قابل کامیت است، `.env` نه.

---

## ۵. قواعد غیرقابل مذاکره

اینها از نسخه‌ی ۱ می‌آیند و همچنان برقرارند. هر تصمیمی که با اینها تناقض داشته باشد غلط است:

1. **هیچ خروجی مدل هرگز اجرا نمی‌شود.** نه `exec`، نه `eval`، نه `$where`، نه `$function`، نه `$accumulator`. مدل فقط داده‌ی ساختاریافته (JSON) تولید می‌کند و آن داده قبل از استفاده اعتبارسنجی می‌شود.
2. **اتصال دیتابیس فقط-خواندنی است.** در مونگو `mode=ro` نداریم، پس این با **کاربر فقط-خواندنی دیتابیس** + **فهرست سفید اپراتورها** تامین می‌شود. هر دو، نه یکی.
3. **هیچ منطقی در لایه‌ی نمایش نیست.** API فقط `Answer` را سریالایز می‌کند.
4. **هیچ مسیر، کلید یا نام مدلی hard-code نمی‌شود.**
5. **همه‌ی پرامپت‌ها در فایل‌های مشخص و قابل بازبینی‌اند** — پرامپت‌های مشترک در `core/prompts.py`، پرامپت مخصوص هر دیتابیس کنار ایجنت خودش.
6. **هر متن غیرقابل‌اعتمادی که وارد پرامپت می‌شود محصور، پاک‌سازی و کوتاه می‌شود.** سه منبع: سوال کاربر، **محتوای دیتابیس**، ساختار نتیجه.
7. **هیچ تابع سطح بالایی exception پرتاب نمی‌کند.** خطا داخل `Answer` برمی‌گردد.
8. **هر قدم باید مستقل قابل تست باشد.** کدی که فقط با کل سیستم بالا کار می‌کند، بد طراحی شده است.

### مدل تهدید مخصوص MongoDB

| خطر | چرا خاص مونگو است | دفاع |
|---|---|---|
| `$where`، `$function`، `$accumulator` | جاوااسکریپت دلخواه را **روی سرور دیتابیس** اجرا می‌کنند | فهرست سفید اپراتورها؛ اینها هرگز مجاز نیستند |
| `$out`، `$merge` | یک aggregation **خواندنی** را به نوشتن تبدیل می‌کنند | فهرست سفید stageها + کاربر فقط-خواندنی |
| `$lookup` به کالکشن غیرمجاز | نشت داده‌ی مشتری دیگر | فقط کالکشن‌های مجاز پروفایل |
| pipeline بی‌سقف | مصرف کامل منابع سرور | `$limit` اجباری + `maxTimeMS` |
| تزریق غیرمستقیم پرامپت | متن داخل یک داکیومنت وارد پرامپت خلاصه می‌شود | محصورسازی + هیچ اجرایی از خروجی مدل |

---

## ۶. پشته‌ی فنی

| لایه | انتخاب | یادداشت |
|---|---|---|
| گردش کار | **LangGraph** | گراف صریح، state مشخص، قابل تست و توقف |
| اتصال به مدل و ابزارها | **LangChain** (`langchain-openai`) | فقط اجزا، نه `AgentExecutor` جادویی |
| دیتابیس اول | **MongoDB** (`pymongo`) | |
| حافظه‌ی دانش | **Chroma** از طریق LangChain | مثل نسخه‌ی ۱، محلی و روی دیسک |
| مدل زبانی | سرویس سازگار با OpenAI (OpenRouter) | با عوض کردن `base_url` به مدل محلی می‌رود |
| API | **FastAPI** | |
| اعتبارسنجی داده | **Pydantic** | قرارداد کوئری و پروفایل و پاسخ |
| تست | **pytest** | گاردها و اعتبارسنجی‌ها بدون تماس با مدل تست می‌شوند |
| نمودار | **Plotly** | همان روش امن نسخه‌ی ۱: JSON از مدل، رسم توسط ما |

---

## ۷. ساختار هدف پروژه

```
rasam-v2/
├── core/
│   ├── settings.py           ← تنظیمات سراسری از .env
│   ├── profiles.py           ← بارگذاری و اعتبارسنجی پروفایل مشتری
│   ├── llm.py                ← تنها دروازه‌ی خروج به سرویس مدل
│   ├── prompts.py            ← پرامپت‌های مشترک + ابزار محصورسازی
│   ├── knowledge.py          ← ساخت و بازیابی دانش (vector store)
│   ├── charting.py           ← نمودار امن (JSON → plotly)
│   ├── models.py             ← Answer، QueryPlan، State (Pydantic)
│   └── graph.py              ← گراف LangGraph
├── agents/
│   ├── base.py               ← قرارداد DatabaseAgent
│   ├── registry.py           ← نگاشت db_type → کلاس ایجنت
│   └── mongo/
│       ├── agent.py          ← MongoAgent
│       ├── guard.py          ← فهرست سفید و اعتبارسنجی pipeline
│       ├── introspect.py     ← استنتاج schema از نمونه‌ی داکیومنت‌ها
│       └── prompts.py        ← پرامپت مخصوص مونگو
├── api/
│   └── main.py               ← FastAPI
├── customers/
│   └── demo/                 ← مشتری نمونه (داده‌ی نیروگاه خورشیدی)
├── scripts/
│   ├── seed_mongo.py         ← ریختن CSVها در مونگو (فقط توسعه)
│   └── train.py              ← ساخت vector store برای یک مشتری
├── tests/
├── docs/
│   └── v1-reference.md       ← دانش منتقل‌شده از نسخه‌ی ۱
├── docker-compose.yml        ← مونگوی محلی برای توسعه
├── .env.example
├── requirements.txt
├── README.md
└── PLAN.md                   ← همین فایل
```

---

## ۸. پروتکل کار

1. من **یک قدم** را پیاده می‌کنم — نه بیشتر.
2. کد را با کامنت و docstring فارسی می‌نویسم که **چرایی** هر تصمیم را توضیح دهد.
3. فایل‌ها را تحویل می‌دهم و خلاصه‌ای می‌نویسم از: چه ساخته شد، چطور تستش کنید، و چه چیزی عمداً هنوز ساخته نشده.
4. شما کد را می‌خوانید. سوال می‌پرسید یا تغییر می‌خواهید.
5. وقتی تایید کردید، من همین فایل (`PLAN.md`) را به‌روز می‌کنم: وضعیت قدم، هر تصمیمی که سر راه گرفته شد، و هر انحرافی از نقشه.
6. قدم بعدی شروع می‌شود.

**اندازه‌ی هر قدم:** حدود ۱۰۰ تا ۲۵۰ خط کد — اندازه‌ای که در یک نشست خوانده و فهمیده شود.

---

## ۹. قدم‌ها

وضعیت‌ها: `⬜ شروع‌نشده` · `🔄 در جریان` · `✅ تایید شده`

---

### ✅ قدم ۱ — اسکلت پروژه و لایه‌ی تنظیمات

**هدف:** پوسته‌ی پروژه، مدیریت تنظیمات سراسری، و بارگذاری پروفایل مشتری با اعتبارسنجی Pydantic.

**فایل‌ها:** `core/settings.py`، `core/profiles.py`، `customers/demo/profile.yaml`، `.env.example`، `requirements.txt`

**معیار پذیرش:** `python -c "from core.profiles import load_profile; print(load_profile('demo'))"` پروفایل معتبر چاپ کند و پروفایل ناقص، خطای واضح فارسی بدهد.

---

### ✅ قدم ۲ — محیط MongoDB و داده‌ی نمونه

**هدف:** یک مونگوی محلی و پرشده از داده‌ی واقعی، تا بقیه‌ی قدم‌ها روی چیز واقعی تست شوند.

**فایل‌ها:** `docker-compose.yml`، `scripts/seed_mongo.py`، `tests/test_seed_mongo.py` (اضافه بر نقشه: تبدیل CSV تابع خالص است و بدون تست خودکار می‌شکند بی‌آنکه کسی متوجه شود)

**کارها:** چهار CSV نیروگاه → چهار کالکشن در دیتابیس `solar_telemetry`. ساخت کاربر فقط-خواندنی از روی همان URI که پروفایل استفاده می‌کند. تبدیل درست انواع (ستونی نه سلولی؛ ستون کاملاً خالی وارد نمی‌شود).

**معیار پذیرش:** `docker compose up -d` بالا بیاید، `python scripts/seed_mongo.py` چهار کالکشن با تعداد ردیف درست بسازد، و اتصال با کاربر فقط-خواندنی نتواند بنویسد. — **تایید شد.** روی مونگوی واقعی (نسخه‌ی ۸٫۳٫۸) با هر چهار کالکشن تایید شد: تعداد داکیومنت‌ها با CSV برابر (۱۰۱۶۱ / ۱۰۱۴۴ / ۲۱۱۲۲ / ۱۰۱۶۹)، `insert` و `delete` هر دو با کاربر `rasam_ro` رد شدند (کد خطای ۱۳: not authorized).

> یادداشت: نام فایل‌های CSV (`solar_telemetry.inverter_data.csv`) الگوی export مونگو است — این داده از اول در مونگو بوده.

---

### ⬜ قدم ۳ — استنتاج schema از MongoDB

**هدف:** مونگو schema ندارد؛ باید از روی داده استنتاجش کنیم.

**فایل‌ها:** `agents/mongo/introspect.py`

**کارها:** نمونه‌گیری از N داکیومنت هر کالکشن، استخراج فیلدها (شامل تودرتو با نقطه)، نوع غالب هر فیلد، درصد حضور، و چند مقدار نمونه برای فیلدهای دسته‌ای.

**معیار پذیرش:** خروجی برای `inverter_data` همه‌ی فیلدهای شناخته‌شده را با نوع درست بدهد، و فیلدهای کم‌حضور علامت‌گذاری شوند. **بدون تماس با مدل زبانی.**

**منبع:** `docs/v1-reference.md` بخش ۱٫۲ (فهرست فیلدهای هر کالکشن) و ۱٫۴ (تکلیف `timestamp` در برابر `ts`).

---

### ⬜ قدم ۴ — گارد امنیتی MongoDB

**هدف:** دروازه‌ای که هیچ pipeline خطرناکی از آن رد نشود. **قلب امنیتی محصول.**

**فایل‌ها:** `agents/mongo/guard.py`، `tests/test_mongo_guard.py`

**کارها:** فهرست سفید stageها (`$match`, `$group`, `$sort`, `$limit`, `$project`, `$addFields`, `$unwind`, `$count`, `$bucket`, `$facet`) و اپراتورها. رد کردن قطعی `$where`, `$function`, `$accumulator`, `$out`, `$merge`. محدود کردن `$lookup` به کالکشن‌های مجاز. تزریق `$limit` اجباری. بررسی بازگشتی تودرتو.

**معیار پذیرش:** مجموعه‌ی تست با حداقل ۱۵ مورد — شامل تلاش‌های فرار مثل `$where` تودرتو داخل `$match`، `$out` در انتهای pipeline، و `$lookup` به کالکشن غیرمجاز. **بدون تماس با مدل زبانی.**

---

### ⬜ قدم ۵ — لایه‌ی مدل زبانی

**هدف:** تنها دروازه‌ی خروج به سرویس مدل، این بار با LangChain.

**فایل‌ها:** `core/llm.py`

**کارها:** ساخت `ChatOpenAI` با `base_url` قابل تنظیم، سقف توکن خروجی، خاموش‌کردن reasoning برای مدل‌های هیبریدی، و لاگ کامل پرامپت وقتی حالت verbose روشن است.

**معیار پذیرش:** یک پرسش ساده جواب بگیرد، و با `RASAM_VERBOSE=1` پرامپت کامل و پاسخ خام چاپ شود.

**منبع:** `docs/v1-reference.md` بخش ۵ — شش کشف عملیاتی که هرکدام ساعت‌ها عیب‌یابی گرفته‌اند (خاموش‌کردن reasoning، اجباری بودن `max_tokens`، پاسخ خالی، …). این بخش را قبل از نوشتن کد بخوانید.

---

### ⬜ قدم ۶ — دانش دامنه و بازیابی

**هدف:** همان قابلیتی که وانّا می‌داد، این بار در کنترل خودمان.

**فایل‌ها:** `core/knowledge.py`، `scripts/train.py`، `customers/demo/knowledge/`

**کارها:** سه نوع دانش (schema، مستندات، مثال‌ها) در Chroma، جدا به‌ازای هر مشتری. بازیابی k مورد مرتبط برای هر سوال.

**معیار پذیرش:** `python scripts/train.py demo` دانش را بسازد؛ یک اسکریپت کوچک نشان دهد برای سوال «دمای اینورترها» کالکشن `inverter_data` بازیابی می‌شود نه `mvpanel_data`.

**منبع:** `docs/v1-reference.md` بخش ۱ — **گران‌ترین دارایی نسخه‌ی ۱**. نگاشت واژه‌های فارسی، راهنمای چهار کالکشن، و پنج الگوی سوال. بخش ۱٫۴ می‌گوید هنگام انتقال چه چیزهایی باید عوض شوند (از جمله یک باگ واقعی در مقایسه‌ی `fa_al_code`).

> **ریسک شناخته‌شده:** embedding پیش‌فرض انگلیسی‌محور است و سوال فارسی را خوب بازیابی نمی‌کند. با ۴ کالکشن مشکلی دیده نمی‌شود؛ با ۵۰ کالکشنِ مشتری، فاجعه است. در همین قدم مدل چندزبانه را امتحان و نتیجه را مقایسه می‌کنیم.

---

### ⬜ قدم ۷ — MongoAgent

**هدف:** اولین ایجنت واقعی — اتصال قدم‌های ۳ تا ۶.

**فایل‌ها:** `agents/base.py`، `agents/registry.py`، `agents/mongo/agent.py`، `agents/mongo/prompts.py`

**کارها:** تعریف قرارداد `DatabaseAgent`، رجیستری، و پیاده‌سازی مونگو: پرامپت تولید pipeline، parse کردن JSON خروجی مدل، و اتصال به گارد و اجرا.

**معیار پذیرش:** یک اسکریپت کوتاه سوال «میانگین دمای داخلی اینورترها چقدر است؟» را به pipeline معتبر تبدیل، اعتبارسنجی و اجرا کند و DataFrame بدهد.

**منبع:** `docs/v1-reference.md` بخش ۳٫۱ (شش قاعده‌ی امنیتی پرامپت — قاعده‌ی ۲ برای مونگو بازنویسی شود، بقیه تقریباً دست‌نخورده) و بخش ۲ (الگوی محصورسازی).

---

### ⬜ قدم ۸ — گراف LangGraph

**هدف:** اتصال همه‌چیز در یک گردش کار صریح.

**فایل‌ها:** `core/models.py`، `core/graph.py`

**کارها:** تعریف `State` و `Answer` با Pydantic، گره‌های `load_profile` تا `execute`، و مسیرهای خطا. هنوز بدون خلاصه و نمودار.

**معیار پذیرش:** `ask("demo", "...")` یک `Answer` کامل برگرداند و هیچ مسیری exception پرتاب نکند — حتی با سوال خالی، سوال بی‌ربط، یا مونگوی خاموش.

**منبع:** `docs/v1-reference.md` بخش ۴ — شکل `Answer` و سه تمایزی که اگر رعایت نشوند رابط کاربری بد می‌شود (`message` در برابر `error`، و `notes` در برابر `error`).

---

### ⬜ قدم ۹ — خلاصه‌سازی امن

**هدف:** خلاصه‌ی فارسی از نتیجه، با محصورسازی محتوای دیتابیس.

**فایل‌ها:** `core/prompts.py`، گره `summarize` در گراف

**معیار پذیرش:** یک داکیومنت آزمایشی با متن مخرب («دستورهای قبلی را نادیده بگیر…») در یک فیلد بگذارید؛ خلاصه باید آن را به‌عنوان یک **مقدار متنی** توصیف کند، نه اجرایش کند.

**منبع:** `docs/v1-reference.md` بخش ۳٫۲ — متن کامل پرامپت خلاصه‌سازی. قید «حداکثر دو جمله و ۴۰ کلمه» به‌همراه جفت مثال درست/غلط حاصل تجربه است؛ **حذفش نکنید.**

---

### ⬜ قدم ۱۰ — نمودار امن

**هدف:** انتقال روش امن نسخه‌ی ۱ (JSON از مدل، رسم توسط ما).

**فایل‌ها:** `core/charting.py`، گره `chart`

**معیار پذیرش:** ستونی که مدل از خودش ساخته باشد در اعتبارسنجی حذف شود؛ نتیجه‌ی تک‌مقداری اصلاً به مدل فرستاده نشود.

**منبع:** `docs/v1-reference.md` بخش ۳٫۳ — قالب JSON، قواعد انتخاب نوع نمودار، و دو نکته‌ی معماری (اعتبارسنجی در برابر ستون‌های واقعی، و بررسی محلی رایگان قبل از تماس با مدل).

---

### ⬜ قدم ۱۱ — قابلیت‌های مشتری

**هدف:** آنچه محصول را «قابل فروش به چند مشتری» می‌کند.

**فایل‌ها:** `core/graph.py`، `core/profiles.py`

**کارها:** ساخت گراف بر اساس `capabilities` پروفایل. مشتری بدون `chart` نباید گره نمودار داشته باشد — نه اینکه داشته باشد و رد شود.

**معیار پذیرش:** دو پروفایل با قابلیت‌های متفاوت، دو گراف متفاوت بسازند و پاسخ‌ها فرق کنند.

---

### ⬜ قدم ۱۲ — خودترمیمی کوئری

**هدف:** اگر اجرا خطا داد، خطا به مدل برگردد تا اصلاح کند (حداکثر ۲ تلاش).

**فایل‌ها:** گره `repair` در گراف

**معیار پذیرش:** سوالی که عمداً به فیلد ناموجود اشاره کند، در تلاش دوم درست اجرا شود و در `Answer.notes` ثبت شود که ترمیم انجام شد.

---

### ⬜ قدم ۱۳ — API

**هدف:** محصول قابل استفاده از بیرون.

**فایل‌ها:** `api/main.py`

**کارها:** `POST /ask`، `GET /health`، `GET /customers/{name}/capabilities`. احراز هویت با API key به‌ازای مشتری.

**معیار پذیرش:** همان سوالات از طریق `curl` پاسخ یکسان بدهند، و مشتری A نتواند با کلید خودش داده‌ی مشتری B را بخواند.

---

### ⬜ قدم ۱۴ — ایجنت دوم (اثبات معماری)

**هدف:** ثابت کردن اینکه افزودن دیتابیس واقعاً «یک فایل» است.

**فایل‌ها:** `agents/postgres/`

**معیار پذیرش:** **هیچ فایلی خارج از `agents/postgres/` تغییر نکند** به‌جز یک خط در رجیستری. اگر لازم شد چیز دیگری عوض شود، یعنی قرارداد ایجنت اشتباه طراحی شده و باید همان‌جا اصلاحش کنیم.

---

### ⬜ قدم ۱۵ — مجموعه‌ی ارزیابی

**هدف:** بدانیم تغییرات کیفیت را بهتر می‌کنند یا بدتر.

**فایل‌ها:** `tests/golden_questions.yaml`، `scripts/evaluate.py`

**معیار پذیرش:** یک دستور، درصد موفقیت روی ۳۰ سوال را گزارش کند.

> این قدم را شاید بخواهید زودتر بیاورید — بدون آن، هر تغییری در پرامپت یک قمار است.

---

### قدم‌های بعدی (هنوز طراحی نشده)

تاریخچه و بازخورد 👍/👎 · صادرات Excel · Docker برای استقرار · کنترل هزینه و توکن · مدل محلی · رابط گرافیکی

---

## ۱۰. لاگ تصمیم‌ها

| # | تصمیم | دلیل | تاریخ |
|---|---|---|---|
| ۱ | حذف وانّا | فرض «خروجی = SQL» در قلبش سیم‌کشی شده؛ برای چنددیتابیسی سقف است | ۱۴۰۵/۰۶/۰۷ |
| ۲ | LangGraph به‌جای AgentExecutor | گردش کار باید صریح و خواندنی باشد، نه پنهان در کتابخانه | ۱۴۰۵/۰۶/۰۷ |
| ۳ | pipeline به‌صورت JSON با فهرست سفید | تنها روشی که هم انعطاف دارد و هم قابل اعتبارسنجی است | ۱۴۰۵/۰۶/۰۷ |
| ۴ | `validate_query` داخل ایجنت، اجبارش در گراف | قواعد امنیتی هر دیتابیس فرق دارد، ولی *الزام* اعتبارسنجی مشترک است | ۱۴۰۵/۰۶/۰۷ |
| ۵ | مونگوی محلی از روی CSVها | مستقل از سرور مشتری، قابل تکرار، و همان داده‌ی واقعی | ۱۴۰۵/۰۶/۰۷ |
| ۶ | کد نسخه‌ی ۱ کپی نمی‌شود، ولی دانشش در `docs/v1-reference.md` منتقل می‌شود | اشتراک فایل‌ها ناچیز است، ولی دانش دامنه و پرامپت‌های آزموده‌شده گران‌اند و ربطی به وانّا ندارند. ریپوی نسخه‌ی ۱ فریز و به‌عنوان خط پایه‌ی مقایسه نگه داشته می‌شود (`git tag v1-vanna`) | ۱۴۰۵/۰۶/۰۷ |
| ۷ | CSVهای خام در `data/raw/` نگه‌داری می‌شوند نه در ریپو | مسیر از قبل در `.gitignore` بود؛ داده‌ی حجیم (~۱۵ مگابایت) و بازتولیدشدنی نباید کامیت شود. هرکس روی سیستم جدید کار کند باید CSVها را جدا تهیه کند | ۱۴۰۵/۰۶/۱۱ |
| ۸ | کاربر فقط-خواندنی مونگو از روی همان `uri_env` پروفایل مشتری ساخته می‌شود، نه از متغیر جدا | تضمین می‌کند رشته‌ی اتصالی که خود محصول مصرف می‌کند واقعاً همان چیزی است که تست شده؛ اسکریپت seed بعد از ساخت کاربر با همان URI وصل می‌شود و نوشتن را امتحان می‌کند | ۱۴۰۵/۰۶/۱۱ |

---

## ۱۱. سوالات باز

- [ ] احراز هویت API: کلید ساده به‌ازای مشتری کافی است یا JWT لازم است؟ (تا قدم ۱۳ وقت داریم)
- [ ] کنترل دسترسی سطری (`plant_id`) در چه قدمی وارد شود؟ نسخه‌ی ۱ این را نداشت و یک بدهی امنیتی بود.
- [ ] embedding چندزبانه: مدل محلی یا سرویس ابری؟ (در قدم ۶ تصمیم می‌گیریم)
